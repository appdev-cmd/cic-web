import { GitCompareArrows, PackageCheck, ReceiptText } from "lucide-react";
import type { DemoProduct } from "../types";
import { stockLabel } from "../mock-ai";

export function ProductCard({ product, onCompare, onQuote }: { product: DemoProduct; onCompare: (p:DemoProduct)=>void; onQuote:(p:DemoProduct)=>void }) {
  const label=stockLabel(product.stock);
  return <article className="w-72 shrink-0 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_8px_30px_rgba(15,23,42,0.06)]"><div className="grid h-28 place-items-center bg-blue-50"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-white shadow-sm"><PackageCheck className="h-9 w-9 text-blue-600" aria-hidden="true" /></div></div><div className="space-y-3 p-4"><div><p className="font-mono text-[11px] font-bold tracking-wide text-blue-700">{product.productCode}</p><h3 className="mt-1 min-h-10 text-sm font-bold leading-5 text-slate-900">{product.productName}</h3></div><div className="flex items-center justify-between"><strong className="text-lg text-blue-700">{product.price.toLocaleString("vi-VN")}đ</strong><span className={`rounded-full px-2 py-1 text-[11px] font-semibold ${product.stock===0?"bg-rose-50 text-rose-700":product.stock<=5?"bg-amber-50 text-amber-700":"bg-emerald-50 text-emerald-700"}`}>{label}: {product.stock}</span></div><dl className="grid grid-cols-2 gap-2 rounded-lg bg-slate-50 p-2.5 text-xs text-slate-600"><div><dt className="text-slate-400">Công suất</dt><dd className="font-semibold">{product.power}</dd></div><div><dt className="text-slate-400">Bảo hành</dt><dd className="font-semibold">{product.warranty}</dd></div></dl><div className="flex gap-2 border-t border-slate-100 pt-3"><button onClick={()=>onCompare(product)} className="flex min-h-9 flex-1 items-center justify-center gap-1 rounded-lg border border-slate-200 text-xs font-semibold hover:bg-slate-50"><GitCompareArrows className="h-3.5 w-3.5" />So sánh</button><button onClick={()=>onQuote(product)} className="flex min-h-9 flex-1 items-center justify-center gap-1 rounded-lg bg-blue-600 text-xs font-semibold text-white shadow-sm hover:bg-blue-700"><ReceiptText className="h-3.5 w-3.5" />Báo giá</button></div></div></article>;
}
