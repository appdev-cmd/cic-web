import type { DemoProduct } from "../types";

export function ComparisonTable({ products, onClose }: { products: DemoProduct[]; onClose:()=>void }) {
  const fields:[string,(p:DemoProduct)=>string][]=[["Thương hiệu",p=>p.brand],["Giá",p=>`${p.price.toLocaleString("vi-VN")}đ`],["Công suất",p=>p.power],["Trọng lượng",p=>p.weight],["Bảo hành",p=>p.warranty],["Tồn kho",p=>String(p.stock)],["Công dụng",p=>p.useCase],["Tiêu chuẩn",p=>p.standards]];
  return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/50 p-4" role="dialog" aria-modal="true" aria-labelledby="compare-title"><div className="max-h-[90vh] w-full max-w-5xl overflow-auto rounded-2xl bg-white p-6 shadow-xl"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-blue-700">Đối chiếu sản phẩm</p><h2 id="compare-title" className="text-xl font-bold">Bảng so sánh</h2></div><button onClick={onClose} className="rounded-lg border px-3 py-2 text-sm">Đóng</button></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead><tr className="bg-slate-50"><th className="p-3 text-left">Tiêu chí</th>{products.map(p=><th key={p.id} className="p-3 text-left">{p.productName}</th>)}</tr></thead><tbody className="divide-y">{fields.map(([label,get])=><tr key={label}><th className="p-3 text-left text-slate-500">{label}</th>{products.map(p=><td key={p.id} className="p-3">{get(p)||"Chưa có thông tin"}</td>)}</tr>)}</tbody></table></div><div className="mt-5 rounded-xl bg-blue-50 p-4 text-sm text-blue-900"><strong>Gợi ý lựa chọn:</strong> {products.slice().sort((a,b)=>a.price-b.price)[0]?.productName} có giá thấp nhất; hãy cân nhắc tồn kho và yêu cầu công suất trước khi quyết định.</div></div></div>;
}
