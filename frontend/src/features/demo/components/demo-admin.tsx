"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { CalendarDays, MessageSquareText, RefreshCcw, ReceiptText, ThumbsUp, UsersRound } from "lucide-react";
import { demoStore } from "../storage";

export function DemoAdmin() {
  const [mounted,setMounted]=useState(false); const [,refresh]=useState(0); useEffect(()=>{demoStore.seed();const timer=window.setTimeout(()=>setMounted(true),0);const fn=()=>refresh(x=>x+1);window.addEventListener("demo-storage",fn);return()=>{window.clearTimeout(timer);window.removeEventListener("demo-storage",fn)}},[]);
  if(!mounted)return <div className="grid min-h-screen place-items-center bg-slate-100 text-sm text-slate-500" role="status">Đang tải dữ liệu demo…</div>;
  const conversations=demoStore.conversations(), leads=demoStore.leads(), quotations=demoStore.quotations(), appointments=demoStore.appointments(), feedback=demoStore.feedback(); const helpful=feedback.filter(x=>x.helpful).length;
  const cards=[{label:"Hội thoại",value:conversations.length,icon:MessageSquareText},{label:"Khách hàng tiềm năng",value:leads.length,icon:UsersRound},{label:"Yêu cầu báo giá",value:quotations.length,icon:ReceiptText},{label:"Lịch tư vấn",value:appointments.length,icon:CalendarDays},{label:"Phản hồi hữu ích",value:helpful,icon:ThumbsUp}];
  return <div className="min-h-screen bg-slate-100"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><div><p className="text-sm font-semibold text-blue-700">CIC Demo Console</p><h1 className="text-xl font-bold tracking-tight">Tổng quan quản trị</h1></div><div className="flex gap-2"><button onClick={()=>{if(confirm("Reset toàn bộ dữ liệu demo?"))demoStore.reset()}} className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-3 py-2 text-xs font-semibold hover:bg-slate-50"><RefreshCcw className="h-4 w-4"/>Reset dữ liệu</button><Link href="/demo" className="rounded-lg bg-blue-600 px-3 py-2 text-xs font-semibold text-white shadow-sm hover:bg-blue-700">Về chatbot</Link></div></div></header><main className="mx-auto max-w-7xl space-y-6 p-4 sm:p-6"><section className="overflow-hidden rounded-2xl bg-slate-900 px-6 py-7 text-white shadow-[0_16px_50px_rgba(15,23,42,0.18)] sm:px-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><div><span className="rounded-full bg-white/10 px-3 py-1 text-[11px] font-bold uppercase tracking-wider text-blue-200">Live demo workspace</span><h2 className="mt-4 text-2xl font-bold tracking-tight sm:text-3xl">Hoạt động tư vấn hôm nay</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-300">Theo dõi hành trình từ câu hỏi sản phẩm đến lead, báo giá và lịch hẹn trong một màn hình trình diễn.</p></div><div className="flex items-center gap-2 text-xs font-semibold text-emerald-300"><span className="h-2 w-2 rounded-full bg-emerald-400"/>Các dịch vụ demo đang hoạt động</div></div></section><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{cards.map(card=><article key={card.label} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_8px_30px_rgba(15,23,42,0.05)]"><div className="flex items-center justify-between"><div className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50"><card.icon className="h-5 w-5 text-blue-600"/></div><span className="text-3xl font-bold tracking-tight">{card.value}</span></div><p className="mt-4 text-xs font-semibold text-slate-500">{card.label}</p></article>)}</div><section className="grid gap-6 lg:grid-cols-2"><DataPanel title="Leads mới" empty="Chưa có lead. Hãy chạy kịch bản gặp nhân viên." rows={leads.map(x=>[x.id,x.name,maskPhone(x.phone),x.status])}/><DataPanel title="Yêu cầu báo giá" empty="Chưa có yêu cầu báo giá." rows={quotations.map(x=>[x.id,x.product,`${x.total.toLocaleString("vi-VN")}đ`,x.status])}/><DataPanel title="Lịch tư vấn" empty="Chưa có lịch tư vấn." rows={appointments.map(x=>[x.id,x.customerName,`${x.date} ${x.time}`,x.channel])}/><DataPanel title="Hội thoại gần đây" empty="Chưa có hội thoại được lưu." rows={conversations.map(x=>[x.id,x.primaryIntent??"—",`${x.messages.length} tin nhắn`,new Date(x.updatedAt).toLocaleString("vi-VN")])}/></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_8px_30px_rgba(15,23,42,0.05)]"><h2 className="font-bold">Trạng thái bản demo</h2><div className="mt-4 grid gap-3 sm:grid-cols-3"><Status label="Intent Router"/><Status label="Product & Legal Mock RAG"/><Status label="LocalStorage persistence"/></div><p className="mt-4 text-xs text-slate-500">Số liệu cập nhật từ các thao tác phát sinh trong chatbot demo và không liên quan dữ liệu Supabase thật.</p></section></main></div>;
}
function maskPhone(value:string){return value.replace(/(\+84\d{2})\d{4}(\d{3})/,"$1****$2")}
function Status({label}:{label:string}){return <div className="flex items-center gap-2 rounded-lg bg-emerald-50 p-3 text-sm font-semibold text-emerald-800"><span className="h-2 w-2 rounded-full bg-emerald-500"/>{label}</div>}
function DataPanel({title,empty,rows}:{title:string;empty:string;rows:string[][]}){return <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-[0_8px_30px_rgba(15,23,42,0.05)]"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><h2 className="font-bold">{title}</h2><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-500">{rows.length} bản ghi</span></div>{rows.length?<div className="overflow-x-auto"><table className="w-full text-sm"><tbody className="divide-y divide-slate-100">{rows.slice(0,6).map((row,i)=><tr key={`${row[0]}-${i}`} className="hover:bg-slate-50">{row.map((cell,j)=><td key={j} className={`px-4 py-3 ${j===0?"font-mono text-xs font-bold text-blue-700":"text-slate-600"}`}>{cell}</td>)}</tr>)}</tbody></table></div>:<div className="p-10 text-center"><div className="mx-auto h-2 w-12 rounded-full bg-slate-100"/><p className="mt-4 text-sm text-slate-500">{empty}</p></div>}</section>}
