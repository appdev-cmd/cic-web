'use client';
import { useState } from 'react';
import { submitContactAction } from '@/features/contact/server/actions';
export function NewsConsultationForm(){const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');if(sent)return <p className="rounded-lg border border-emerald-200 bg-emerald-50 p-4 text-xs font-bold text-emerald-700">Cảm ơn bạn! Yêu cầu tư vấn đã được gửi thành công.</p>;return <form className="space-y-3.5" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{await submitContactAction(Object.fromEntries(new FormData(e.currentTarget)));setSent(true)}catch{setError('Không thể gửi yêu cầu. Vui lòng thử lại.')}finally{setBusy(false)}}}><div className="grid gap-3.5 sm:grid-cols-2"><input name="fullname" required placeholder="Nhập họ và tên" className="rounded-lg border border-slate-200 bg-slate-50 p-2.5 text-xs"/><input name="email" type="email" required placeholder="Nhập email liên hệ" className="rounded-lg border border-slate-200 bg-slate-50 p-2.5 text-xs"/></div><textarea name="message" rows={3} placeholder="Mô tả nhu cầu của bạn..." className="w-full resize-none rounded-lg border border-slate-200 bg-slate-50 p-2.5 text-xs"/><input type="hidden" name="subject" value="Tư vấn từ bài viết"/><button disabled={busy} className="flex w-full items-center justify-center rounded-lg bg-[#FC5115] py-3 text-xs font-bold uppercase tracking-wider text-white disabled:opacity-50">{busy?'Đang gửi thông tin...':'Gửi yêu cầu tư vấn'}</button>{error&&<p className="text-xs text-red-600">{error}</p>}</form>}
