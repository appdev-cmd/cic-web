'use client';
import { useState } from 'react';
import { ChevronDown, ChevronUp, ListOrdered } from 'lucide-react';
export function NewsToc({ items }: { items: Array<{id:string;title:string}> }) { const [open,setOpen]=useState(true); return <section className="overflow-hidden rounded-[10px] border border-slate-200/80 bg-white shadow-sm"><button onClick={()=>setOpen(v=>!v)} className="flex w-full items-center justify-between border-b border-slate-100 p-4 text-left"><span className="flex items-center gap-2 text-xs font-black uppercase tracking-wider text-orange-600"><ListOrdered size={15}/> Mục lục bài viết <b className="rounded bg-slate-100 px-1.5 py-0.5 text-[10px] text-slate-400">{items.length} mục</b></span>{open?<ChevronUp size={16}/>:<ChevronDown size={16}/>}</button>{open&&<nav className="space-y-1 p-4">{items.map((x,i)=><a key={x.id} href={`#${x.id}`} className="block rounded px-2 py-1.5 text-xs text-slate-600 hover:bg-orange-50 hover:text-orange-600"><span className="mr-1.5 font-bold text-orange-600">{i+1}.</span>{x.title}</a>)}</nav>}</section> }
