import { Building2, CalendarDays, Package } from 'lucide-react';
export function NewsRelatedSections({ projects, events, products }: { projects: any[]; events: any[]; products: any[] }) {
 const groups=[['Dự án liên quan',Building2,projects,'/projects/'],['Sự kiện liên quan',CalendarDays,events,'/events/'],['Sản phẩm liên quan',Package,products,'/products/']] as const;
 return <div className="space-y-8">{groups.map(([label,Icon,items,prefix])=>items.length>0&&<section key={label}><h2 className="mb-4 flex items-center gap-2 text-lg font-black text-slate-900"><Icon size={19} className="text-orange-600"/>{label}</h2><div className="grid gap-4 sm:grid-cols-2">{items.slice(0,4).map((x:any)=><a key={x.id} href={`${prefix}${x.slug??x.alias??x.id}`} className="group flex gap-3 rounded-[10px] border border-slate-200 bg-white p-3.5 shadow-sm transition hover:-translate-y-0.5 hover:border-orange-500 hover:shadow-md">{x.image&&<img src={x.image} alt="" className="h-16 w-16 shrink-0 rounded object-cover"/>}<div className="min-w-0"><h3 className="line-clamp-2 text-xs font-bold text-slate-900 group-hover:text-orange-600">{x.title??x.name??x.code}</h3><p className="mt-1 line-clamp-2 text-[11px] text-slate-500">{x.summary??x.description??''}</p></div></a>)}</div></section>)}</div>;
}
