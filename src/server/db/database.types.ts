/**
 * Placeholder for generated Supabase database types.
 * Replace this file through the approved schema/type generation workflow;
 * application UI must not import generated database row types directly.
 */
export type Database = Record<string, never>;
