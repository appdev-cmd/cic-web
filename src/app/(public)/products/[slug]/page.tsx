/* eslint-disable @next/next/no-img-element */
import { notFound } from 'next/navigation';
import { getPublishedProductBySlug } from '@/features/products/server/queries';
export const dynamic = 'force-dynamic';
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) { const product = await getPublishedProductBySlug((await params).slug); return product ? { title: `${product.title} | CIC`, description: product.summary ?? undefined } : {}; }
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) { const product = await getPublishedProductBySlug((await params).slug); if (!product) notFound(); return <article className="mx-auto max-w-5xl px-4 py-10 sm:px-6 sm:py-16"><nav className="text-sm text-slate-500" aria-label="Breadcrumb"><a href="/products" className="hover:text-orange-600">Sản phẩm</a><span className="mx-2">/</span><span>{product.title}</span></nav><div className="mt-8 grid gap-10 lg:grid-cols-[1.15fr_1fr] lg:items-start">{product.image && <img src={product.image} alt={product.title} className="aspect-[4/3] w-full rounded-2xl object-cover" />}<div><h1 className="text-3xl font-extrabold tracking-tight sm:text-4xl">{product.title}</h1>{product.summary && <p className="mt-6 text-lg leading-8 text-slate-600">{product.summary}</p>}<a href="/contact" className="mt-8 inline-flex rounded-xl bg-orange-600 px-5 py-3 font-semibold text-white transition hover:bg-orange-700">Liên hệ tư vấn</a></div></div></article>; }
